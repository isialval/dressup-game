{
  "name": "dressup",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "gen:catalog": "node scripts/generateCatalog.mjs",
    "dev": "npm run gen:catalog && vite --host",
    "build": "npm run gen:catalog && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "pixi.js": "^8.15.0"
  }
}
